// 用途：Agent 记忆板块，分类查看、编辑、删除记忆条目。
import AppShell from "@/components/layout/app-shell";

export const metadata = {
  title: "Agent 记忆 · TikTok 跨境电商工作台",
};

export default function Page() {
  return (
    <AppShell>
      <div className="flex h-full items-center justify-center text-sm text-gray-400">Agent 记忆管理（占位，待开发）</div>
    </AppShell>
  );
}
