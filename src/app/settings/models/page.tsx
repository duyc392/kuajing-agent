// 用途：模型与 API Key 板块，查看和修改对话、生图、FastMoss 三个 Key 及模型选择。
import Header from "@/components/layout/header";

export const metadata = {
  title: "模型与 API Key · TikTok 跨境电商工作台",
};

export default function Page() {
  return (
    <main className="min-h-screen bg-gray-50">
      <Header />
      <div className="mx-auto max-w-5xl px-6 py-16 text-center text-sm text-gray-400">模型与 API Key（占位，复用引导页表单）</div>
    </main>
  );
}
