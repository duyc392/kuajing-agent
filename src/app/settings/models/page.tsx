// 用途：模型与 API Key 板块，查看和修改对话、生图、FastMoss 三个 Key 及模型选择。
import AppShell from "@/components/layout/app-shell";

export const metadata = {
  title: "模型与 API Key · TikTok 跨境电商工作台",
};

export default function Page() {
  return (
    <AppShell>
      <div className="flex h-full items-center justify-center text-sm text-gray-400">模型与 API Key（占位，复用引导页表单）</div>
    </AppShell>
  );
}
