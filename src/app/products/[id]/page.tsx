// 用途：商品详情页，含"文案历史"标签页（所有版本、对比、切换）和变体 SKU 管理。
import AppShell from "@/components/layout/app-shell";

export default function ProductDetailPage() {
  return (
    <AppShell>
      <div className="flex h-full items-center justify-center text-sm text-gray-400">商品详情（占位，待开发）</div>
    </AppShell>
  );
}
