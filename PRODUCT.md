# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

独立跨境电商卖家（单人使用），在 TikTok 平台开店运营，需要 AI 助手完成选品调研、商品文案与素材生成、内容策划与脚本创作、店铺运营优化等工作。应用为本地部署、单机使用，数据全部存在卖家自己的电脑上（127.0.0.1，无账号体系）。

## Product Purpose

TikTok 跨境电商 AI 运营工作台：核心是一个具备领域知识、长期记忆和持续学习能力的 Agent（AI 智能助手），卖家通过自然语言对话完成四类工作——选品调研、商品文案与素材生成、内容策划与脚本创作、店铺运营优化。数据全部存在本机。

## Positioning

开源的本地部署工作台：数据不出本机（SQLite 单文件 + 本地文件），Agent 对话式完成跨境电商运营全流程，能力可持续沉淀（长期记忆 + 可复用技能 + 领域知识问答）。

## Operating Context

- 本地部署：`next dev` 运行在 127.0.0.1，单用户浏览器访问，无账号、无鉴权。
- 主要流程：配置 API Key（DeepSeek 对话/生图、FastMoss 数据）→ 创建店铺 → 工作台对话/选品/文案/脚本 → 数据看板 → 设置管理。
- 主要页面：工作台（对话 + 侧边对话列表 + 快捷指令 + 全局 Agent 抽屉）、商品、脚本、数据看板、设置（店铺管理 / 模型与 API Key / Agent 记忆 / 技能管理 / 数据导出）。
- 使用场景：桌面浏览器为主（卖家在电脑前操作），需兼顾窄屏。

## Capabilities and Constraints

- 能力：对话式 Agent（工具调用过程可见）、选品调研（FastMoss 实时数据）、商品文案 / 视频脚本 / 直播脚本生成、商品与变体管理、数据看板（FastMoss 实时查询，不落库）、长期与短期记忆、技能沉淀、Excel 数据导出、领域知识问答（轻量 RAG）。
- 技术约束：Next.js 14 App Router + React 18 + TypeScript strict + Tailwind CSS 3 + Prisma/SQLite；无 UI 组件库、无图表库（图表为手写 SVG）；shopId 隔离铁律；组件只经 apiRequest 封装调接口；页面/组件与服务层/Agent 层严格分层。
- 语言：界面与 Agent 回答默认中文。

## Brand Commitments

无既定品牌资产（无 Logo、无指定配色、无字体）。现状为 Tailwind 默认蓝/灰视觉；用户已授权重新设计，主色是否保留蓝色为未决事项（由设计方向轮次决定）。

## Evidence on Hand

PRD-v2.txt（完整需求规格，位于项目根目录）；AGENTS.md（工程规范）；种子演示数据（店铺 / 商品 / 脚本 / 对话 / 工具调用记录）。无真实用户数据、无历史界面截图存档。

## Product Principles

1. 单人本地优先：一切功能不依赖云端、不假设多人协作与权限体系。
2. Agent 是核心交互：自然语言驱动，工具调用过程可见，数据据实回答不编造。
3. 店铺隔离是铁律：任何展示与操作不得跨店铺串数据。
4. 功能完整与可靠优先于视觉华丽，但视觉质感不应拖累工具的信任感。
5. 领域知识、记忆、技能都可沉淀复用，界面应让这些资产可被发现、可管理。

## Accessibility & Inclusion

无既定无障碍标准；界面中文，键盘可达性（焦点可见）与窄屏可用为基本要求。
